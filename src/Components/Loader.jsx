
// Simple loading indicator shown while waiting for AI response
const Loader = () => {

    return (
        <div className="text-gray-500 italic text-sm animate-pulse">
            AI is typing...
        </div>
    );

};

export default Loader;